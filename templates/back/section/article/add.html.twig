{% extends 'base-back.html.twig' %}

{% block title %}Add Article to section {{ section.name }}{% endblock %}

{% block body %}
<h1>Ajouter un article à la section {{ section.name }}</h1>

{# todo détailler l'affichage du formulaire #}
{{ form_start(form, {"attr": {"novalidate": "novalidate"}}) }}
<button class="btn btn-primary">Ajouter</button>
{{ form_end(form) }}
{% endblock %}
